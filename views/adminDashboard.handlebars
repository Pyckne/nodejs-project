<section class="section">
  <div class="container" style="max-width: 800px;">
    <h1 class="title is-3">👑 Panel de Administrador</h1>
    <p class="subtitle is-5">Bienvenido, {{user.first_name}} {{user.last_name}}</p>

    {{#if (eq user.role "admin")}}
      <div class="box">
        <h2 class="title is-5">🛠 Herramientas de administración</h2>

        <div class="buttons is-flex-direction-column is-align-items-flex-start">
          <a class="button is-light is-link" href="/realtimeproducts">🛠️ Gestión en tiempo real de productos</a>
          <a class="button is-light is-info is-disabled" href="/admin/users">👥 Ver usuarios registrados (pendiente)</a>
          <a class="button is-light is-info is-disabled" href="/admin/create-product">➕ Crear nuevo producto (pendiente)</a>
          <a class="button is-light" href="/home">🏠 Ir al home</a>
        </div>

        <form action="/api/auth/signout" method="POST" class="mt-4">
          <button type="submit" class="button is-danger is-light">Cerrar sesión</button>
        </form>
      </div>
    {{else}}
      <div class="notification is-danger is-light">
        ⚠️ Acceso denegado. Esta sección está restringida a administradores.
      </div>
    {{/if}}
  </div>
</section>